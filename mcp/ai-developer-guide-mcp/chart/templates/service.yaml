apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-developer-guide-mcp.fullname" . }}
  labels:
    {{- include "ai-developer-guide-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ai-developer-guide-mcp.selectorLabels" . | nindent 4 }}
